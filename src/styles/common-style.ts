import { StyleSheet } from "react-native";

/**
 * 共通のスタイリング
 * @author kotatanaka
 */
export const appStyle = StyleSheet.create({
  defaultContainer: {
    flex: 1,
    paddingTop: 15,
    backgroundColor: "#fff",
    justifyContent: "center"
  }
});
