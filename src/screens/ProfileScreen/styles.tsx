import { StyleSheet } from 'react-native';

/**
 * ProfileScreenのスタイリング
 */
const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 15,
    backgroundColor: '#fff',
    justifyContent: 'center',
  },
});

export default styles;
